# Task Completion Checklist

1. Run `bun run validate:all` (ESLint + TypeScript check)
2. Run `bun run format:all:check` to verify formatting
3. Run `bun run test:all` if tests are applicable
4. Ensure conventional commit format if committing
